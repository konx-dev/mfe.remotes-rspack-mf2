# Module Federation 2.0 Module MonoRepo with Turbo

## Folder structure

`/apps` - These are the things that actually get deployed and turborepo entry points.

`packages` - These are shared libraries that apps import (UI kits, utils, configs).