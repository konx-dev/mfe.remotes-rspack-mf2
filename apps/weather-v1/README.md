# Weather Module v1 (MFE Remote)

A standalone Weather Widget built with **React 19** and **Rspack**.

## Setup
1. Create a `.env` file based on `.env.example`.
2. Install dependencies at the root.

## Development
Run this module standalone:
`npx turbo dev --filter=@mfe/weather-v1`